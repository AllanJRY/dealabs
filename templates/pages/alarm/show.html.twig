{% extends '/pages/profil/layout_profil.html.twig' %}

{% block title %}Alarm{% endblock %}

{% block main_profile_section %}
    <h1>Alarm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alarm.id }}</td>
            </tr>
            <tr>
                <th>Search</th>
                <td>{{ alarm.search }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ alarm.rating }}</td>
            </tr>
            <tr>
                <th>Mail_notif</th>
                <td>{{ alarm.mailNotif ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('alarm_index') }}">back to list</a>

    <a href="{{ path('alarm_edit', {'id': alarm.id}) }}">edit</a>

    {{ include('pages/alarm/_delete_form.html.twig') }}
{% endblock %}
