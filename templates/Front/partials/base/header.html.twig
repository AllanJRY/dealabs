<header>
    <div class="container">
        <div class="main-header-flex-container">
            <div class="top-logo-btn-container">
                <div class="site-logo-container">
                    <a href="{{ path('home') }}">
                        Home
{#                        {% if get_option('Parametre').getTag('Logo-du-site') %}#}
{#                            <img src="{{ get_option('Parametre').getTag('Logo-du-site') }}" alt="">#}
{#                        {% endif %}#}
                    </a>
                </div>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>
            <div class="d-flex">
                <nav class="navbar navbar-expand-lg navbar-light">
                    <div class="collapse navbar-collapse" id="navbarNav">
{#                        {{ knp_menu_render('frontMenu',{'currentClass': 'active', 'allow_safe_labels': true}) }}#}
                    </div>
                </nav>
            </div>
        </div>
    </div>
</header>
