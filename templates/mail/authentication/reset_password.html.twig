{% extends 'mail/email_base.html.twig' %}

{% block subject -%}
{#    {{ 'security.reset_password.pages.email.subject'|trans({}, 'security') }}#}
    Dealabs - Reset password
{%- endblock subject %}

{% block html %}
{#    <h1>{{ 'security.reset_password.pages.email.title'|trans({}, 'security') }}</h1>#}

{#    {{ 'security.reset_password.pages.email.content' | trans({'%url%': url('app_reset_password', {token: resetToken.token}), '%token_lifetime%': tokenLifetime|date('g')}, 'security') | raw }}#}

    <h1>Hi!</h1>

    <p>To reset your password, please visit the following link</p>

    <a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

    <p>This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

    <p>Cheers!</p>
{% endblock html %}

{% block text %}{% endblock text %}
